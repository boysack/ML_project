from utils import *
from plots import *

if __name__=="__main__":
  dtr,ltr = load_csv('data/Train.txt')
  # generate_and_save_features_hists(z_normalization(dtr), ltr, './plots/')
  # generate_and_save_features_scatter_plots(z_normalization(dtr), ltr, './plots/')

  
